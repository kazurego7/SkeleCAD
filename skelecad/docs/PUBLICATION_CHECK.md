# ソース公開準備の検証記録

実施日: 2026-09-05。ソース配布の準備を対象とし、リモートへの送信は行っていません。

これは初回の公開準備時点の記録です。同日その後に行ったツール・依存ライブラリの更新と
再検証は[依存更新記録](DEPENDENCY_UPDATE.md)を参照してください。

## 変更

- ルートをGitリポジトリとして初期化し、ブランチ名を`main`に設定。
- 実行環境、ブラウザプロファイル、生成物、画像入力、バックアップ、旧試作を除外。
- 個人のTailscaleホスト名をドキュメントの一般的なプレースホルダーに変更。
- README、導入・公開手順、外部依存の説明、改行設定、GitHub Actionsを追加。
- 公開対象のインデックスを検査するスクリプトとソース専用のテスト入口を追加。
- プロセス起動を模擬する1テストを、手元のPython配置に依存しない形に修正。

## 実行結果

| 検証 | 結果 |
| --- | --- |
| 公開対象の禁止パス・代表的な認証情報パターン・個人パス | 合格 |
| 公開するPython/JSONの構文 | 合格 |
| `git diff --cached --check` | 合格 |
| 製作環境でのビュワーテスト | 8ファイル合格 |
| 公開対象だけを取り出したビュワーテスト | 7ファイル合格 |
| 製作環境でのCPU画像・メッシュ・印刷処理テスト | 71件合格 |
| 公開対象だけを取り出したCPUテスト | 初回70件合格・1件失敗。環境依存を修正後、該当モジュールの27件が全件合格 |

公開用コピーには`.tools/`、元画像、既存`build/`を含めていません。
CPUテストの実行にはローカルに導入済みのPython・依存ライブラリを使いました。
新規PCでの依存インストールとGitHub Actions上の実行は未検証です。
パターン照合は秘密情報や素材の権利関係を網羅的に保証するものではありません。

## 製作検証の扱い

形状・材料・寸法パラメータに変更はありません。
CAD、STLトポロジー、組立衝突、CalculiXの全ビルド検証は今回再実行していません。
組立画像の再生成も行っていません。過去の解析記録と今回のソース検証を区別してください。

## 公開時の状態

ソースはステージ済みで、コミット前の確認ができる状態です。
リモートは未登録、外部へのアップロードは未実施です。
独自部分のオープンソースライセンスは未設定です。
次の操作は[公開手順](SETUP.md#gitへの公開)を参照してください。
